# 공공데이터 API 확장 폴더
현재 프로젝트는 첨부된 2021 공공체육시설 Excel을 가공한 JS 데이터를 기본으로 사용합니다.
실시간/최신 API를 연결할 경우 `public-data.config.js`에 endpoint와 serviceKey를 넣고 별도 fetch 모듈을 연결하세요.
API 응답은 화면에 필요한 필드만 객체로 정규화하는 방식을 권장합니다.
